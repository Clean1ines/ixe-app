pytest>=8.0.0
pytest-asyncio>=0.23.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0  # Async driver for SQLite, compatible with SQLAlchemy
# sqlite3 is part of the Python standard library, no need to list it explicitly
# Other potential dependencies like playwright, fastapi, etc., will be added later as needed
